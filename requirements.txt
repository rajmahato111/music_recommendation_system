# Core dependencies - required for basic functionality
streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.26.0
scikit-learn>=1.3.0
scipy>=1.11.0
plotly>=5.17.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Jupyter Notebook support
jupyter>=1.0.0
notebook>=7.0.0
ipykernel>=6.25.0

# Optional dependencies for hybrid recommendation system
# Uncomment the ones you need:

# For Item2Vec collaborative filtering (required for notebook 02)
gensim>=4.3.0

# For ANN indexing (required for notebook 04, faster retrieval)
faiss-cpu>=1.7.4

# For ranking model (optional, for advanced ranking)
# lightgbm>=4.0.0

# Alternative ANN library (optional, alternative to FAISS)
# hnswlib>=0.7.0

# Note: 
# - Install core dependencies: pip install -r requirements.txt
# - For full hybrid system: pip install gensim faiss-cpu
# - For Apple Silicon (M1/M2), you may need: pip install faiss-cpu --no-cache-dir
